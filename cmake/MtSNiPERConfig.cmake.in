###############################################################################
# MtSNiPER CMAKE CONFIGURATION File for External Projects
# This file is generated by MtSNiPER CMake, and SHOULD NOT BE EDITED MANUALLY
#
# Exported Targets
#   MTSNiPER::SniperMuster  - the core component
#
# It also defines the following variables:
#   MT_SNIPER_INCLUDE_DIR    - include directory
#   MT_SNIPER_LIBRARY_DIR    - library directory
#
###############################################################################
@PACKAGE_INIT@

set_and_check(MT_SNIPER_INCLUDE_DIR "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")
set_and_check(MT_SNIPER_LIBRARY_DIR "@PACKAGE_CMAKE_INSTALL_LIBDIR@")

include(CMakeFindDependencyMacro)

if(@USE_PYTHON@)
    find_dependency(Python @PYTHON_VERSION@)
    #find_dependency(Boost_PYTHON @Boost_VERSION@)
endif()

if(@USE_ROOT@)
    find_dependency(ROOT @ROOT_VERSION@)
endif()

if(NOT TARGET MT_SNiPER::SniperMuster)
    include("${CMAKE_CURRENT_LIST_DIR}/MtSNiPERTargets.cmake")
endif()

check_required_components(MtSNiPER)

# Print the default "Found" message and check library location
include(FindPackageHandleStandardArgs)
get_property(TEST_MT_SNIPER_LIBRARY TARGET MT_SNiPER::SniperMuster PROPERTY LOCATION)
find_package_handle_standard_args(MtSNiPER DEFAULT_MSG CMAKE_CURRENT_LIST_FILE TEST_SNIPER_LIBRARY)

